// TODO: Integrar Resend + React Email
// import { Resend } from 'resend';

export const sendEmail = async (to: string, subject: string, html: string) => {
    // Implementar envío de correo
    console.log("Enviando correo a:", to);
    return { success: true };
};
